(function(){'use strict';
const $=s=>document.querySelector(s);let cfg=null,reportData=null;
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
async function config(){const r=await fetch('data/community_config.json?v=20260728-aio-connect',{cache:'no-store'});if(!r.ok)throw new Error('Nie udało się odczytać konfiguracji strony.');return r.json();}
async function api(payload){const name=(cfg.edgeFunctions&&cfg.edgeFunctions.aioConnectReport)||'aio-connect-report';const url=String(cfg.supabase.url).replace(/\/+$/,'')+'/functions/v1/'+name;const r=await fetch(url,{method:'POST',cache:'no-store',headers:{'Content-Type':'application/json','apikey':cfg.supabase.anonKey},body:JSON.stringify(payload)});const raw=await r.text();let data={};try{data=raw?JSON.parse(raw):{};}catch(_){data={error:raw};}if(!r.ok||data.error)throw new Error(data.error||('HTTP '+r.status));return data;}
function showError(message){$('#report-status').innerHTML='<div class="aio-report-error"><strong>Nie udało się otworzyć raportu.</strong><p>'+esc(message)+'</p></div>';$('#manual-report').hidden=false;}
function render(data){reportData=data;$('#report-status').hidden=true;$('#report-editor').hidden=false;const meta=data.meta||{};const pairs=[['Kod urządzenia',meta.device||'—'],['Tuner',meta.model||'—'],['System',meta.system||'—'],['Python',meta.python||'—'],['Kondycja',meta.health!==''&&meta.health!=null?meta.health+'/100':'—']];$('#report-summary').innerHTML=pairs.map(x=>'<div><small>'+esc(x[0])+'</small><strong>'+esc(x[1])+'</strong></div>').join('');$('#technical-report').value=data.report||'';if(meta.model)$('#report-title').value='Problem z '+meta.model+' — raport AIO Connect';if(data.expiresAt){const d=new Date(data.expiresAt);$('#report-expiry').textContent='Raport wygaśnie: '+new Intl.DateTimeFormat('pl-PL',{dateStyle:'medium',timeStyle:'short'}).format(d);}}
async function load(){try{cfg=await config();const p=new URLSearchParams(location.search);const id=p.get('id'),token=p.get('token');if(!id||!token){const meta={device:p.get('device')||'',model:p.get('model')||'',system:p.get('system')||'',python:p.get('python')||'',health:p.get('health')||''};render({meta:meta,report:'',expiresAt:null});$('#manual-report').hidden=false;return;}const data=await api({action:'read',id,token});render(data);}catch(e){showError(e.message||e);}}
async function copyReport(){const text=$('#technical-report').value;try{await navigator.clipboard.writeText(text);window.AIOCommunity&&AIOCommunity.showToast('Raport skopiowany.','success');}catch(_){$('#technical-report').select();document.execCommand('copy');}}
async function publish(){const C=window.AIOCommunity;if(!C||!C.ready){alert('Moduł Społeczności AIO jeszcze się ładuje.');return;}if(!C.user){C.openAuth('Zaloguj się, aby opublikować raport w Społeczności AIO.');return;}const title=$('#report-title').value.trim();const desc=$('#problem-description').value.trim();const report=$('#technical-report').value.trim();if(title.length<6){C.showToast('Uzupełnij tytuł zgłoszenia.','error');return;}if(desc.length<20){C.showToast('Opisz problem w co najmniej 20 znakach.','error');return;}const content='OPIS PROBLEMU\n'+desc+'\n\nRAPORT AIO CONNECT\n'+report;const btn=$('#publish-report');btn.disabled=true;btn.textContent='Publikowanie…';try{const result=await C.edgeCall('write',{action:'create_post',title,content,category:'aio-panel',postType:'problem'});const row=result&&result.data?result.data:null;const msg=row&&row.status==='pending'?'Zgłoszenie zapisano i przekazano do zatwierdzenia.':'Zgłoszenie zostało opublikowane.';$('#report-editor').insertAdjacentHTML('afterbegin','<div class="aio-report-success"><strong>'+esc(msg)+'</strong>'+(row&&row.id?'<p><a class="button primary" href="post.html?id='+encodeURIComponent(row.id)+'">Otwórz wpis</a></p>':'')+'</div>');C.showToast(msg,'success');}catch(e){C.showToast(C.friendlyError(e),'error');}finally{btn.disabled=false;btn.textContent='Opublikuj w Społeczności AIO';}}
document.addEventListener('DOMContentLoaded',()=>{load();$('#copy-report').addEventListener('click',copyReport);$('#publish-report').addEventListener('click',publish);});})();
